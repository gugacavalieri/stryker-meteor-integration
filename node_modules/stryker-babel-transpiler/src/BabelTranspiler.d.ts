import { Transpiler, TranspilerOptions } from 'stryker-api/transpile';
import { File } from 'stryker-api/core';
declare class BabelTranspiler implements Transpiler {
    private babelOptions;
    private projectRoot;
    constructor(options: TranspilerOptions);
    transpile(files: ReadonlyArray<File>): Promise<ReadonlyArray<File>>;
    private transpileFileIfNeeded;
    private transpileFile;
    private determineProjectRoot;
}
export default BabelTranspiler;
