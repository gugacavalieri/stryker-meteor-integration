import { StrykerOptions } from 'stryker-api/core';
import PromptOption from './PromptOption';
export default class StrykerConfigWriter {
    private out;
    private readonly log;
    constructor(out: (output: string) => void);
    guardForExistingConfig(): void;
    /**
    * Create stryker.conf.js based on the chosen framework and test runner
    * @function
    */
    write(selectedTestRunner: null | PromptOption, selectedTestFramework: null | PromptOption, selectedMutator: null | PromptOption, selectedTranspilers: null | PromptOption[], selectedReporters: PromptOption[], additionalPiecesOfConfig: Partial<StrykerOptions>[]): Promise<void>;
    private configureTestFramework;
    private writeStrykerConfig;
    private wrapInModule;
}
