/// <reference types="node" />
/**
 * Creates a new random folder with the specified prefix.
 * @param prefix The prefix.
 * @returns The path to the folder.
 */
declare function createRandomFolder(prefix: string): string;
/**
 * Writes data to a specified file.
 * @param fileName The path to the file.
 * @param data The content of the file.
 * @returns A promise to eventually save the file.
 */
declare function writeFile(fileName: string, data: string | Buffer, instrumenter?: NodeJS.ReadWriteStream | null): Promise<void>;
/**
 * Deletes the Stryker-temp folder
 */
declare function clean(): Promise<void>;
declare const _default: {
    createRandomFolder: typeof createRandomFolder;
    writeFile: typeof writeFile;
    clean: typeof clean;
};
export default _default;
